{
  "benchmarks": [
    {
      "provider": "openai",
      "model": "gpt-4.1",
      "attempts": 1
    },
    {
      // OpenAI Reasoning Models
      "provider": "openai",
      "model": "o4-mini",
      "attempts": 1,
      // "model_config": Optional provider-specific settings.
      // LIMITATION: Only used by "openai" for "reasoning_effort".
      "model_config": {
        "reasoning_effort": "high"
      }
    },
    {
      "provider": "anthropic",
      "model": "claude-3-sonnet-20240229",
      "attempts": 1
      // LIMITATION: `model_config` is not used. "thinking" is hardcoded.
    },
    {
      "provider": "gemini",
      "model": "gemini-2.5-pro",
      "attempts": 1
      // LIMITATION: `model_config` is not used.
    },
    {
      "provider": "openrouter",
      "model": "meta-llama/llama-3-8b",
      "attempts": 1
      // LIMITATION: `model_config` is not used.
    },
    {
      "provider": "openrouter",
      "model": "deepseek/deepseek-prover-v2",
      "attempts": 1
      // LIMITATION: `model_config` is not used.
    }
  ]
}
